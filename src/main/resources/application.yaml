spring:
  application:
    name: Produto API
  datasource:
    url: jdbc:h2:mem:produto
    username: sa
    password: 1806
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
    h2:
      console:
        enabled: true
        path: /h2-console

